import { lib, game, ui, get, ai, _status } from "../../../../noname.js";

const characterSort = {
	yys_jialedi:[],
};

const sortTranslate = {
	//武将包名
	// `${packName}_character_config`
	yys_character_config:"阴阳师",
	//分包翻译
	//yys_jialedi:"yys-迦勒底",
};

export { characterSort, sortTranslate };
